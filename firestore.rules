rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /kb/{document=**} {
      allow read, write: if true; // Restrict in production
    }
    match /daily_posts/{document=**} {
      allow read, write: if true; // Restrict in production
    }
  }
}
